<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity
            android:name=".RecordingActivity"
            android:exported="false" />

        <!-- 커뮤니티 관련 액티비티 등록 -->
        <activity
            android:name=".community.CreatePostActivity"
            android:exported="false" />
        <activity
            android:name=".community.SelectAttachmentActivity"
            android:exported="false" />

        <activity android:name=".question.QuizActivity" />

        <activity android:name=".User.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".User.RegisterActivity" />

        <activity android:name=".community.PostDetailActivity" />

        <activity android:name=".User.EditProfileActivity" />

        <activity android:name=".User.FindAccountActivity" />

    </application>
</manifest>
